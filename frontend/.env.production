# 前端生产构建期注入（公开配置，可入库；无密钥）
# 真实值可在 CloudBase/EdgeOne 控制台覆盖，或在此填写后重新构建。

# 后端 API 基地址：
#  - 推荐（EdgeOne 反代）：保持 /api，由 EdgeOne 将 /api/* 转发到 CloudBase 后端
#  - 或直接指向后端云托管域名：https://<your-cloudbase-service-domain>
VITE_API_BASE=/api

# 部署到 CDN 子路径时设置，例如 /workbench/；根域名部署留空或 /
VITE_BASE=/
